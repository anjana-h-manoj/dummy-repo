1. goto the folder
2. git init 
3. git add . -> add all the files to the repo/
git add filename
4.commit
push -> sending changes from local repo to cloud repo


echo "# dummy-repo" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/anjana-h-manoj/dummy-repo.git
git push -u origin main